
  # Robot configuration
max_vel_x:  0.5 #0.8
min_vel_x: -0.2

max_vel_y: 0.0  # diff drive robot
min_vel_y: 0.0  # diff drive robot

max_speed_xy: 0.5    # max_trans_vel: 0.8  # choose slightly less than the base's capability
min_speed_xy: 0.1    # min_trans_vel: 0.1  # this is the min trans velocity when there is negligible rotational velocity

max_vel_theta: 1.0 #2.5   # max_rot_vel: 1.0  # choose slightly less than the base's capability
min_speed_theta: 0.1  # min_rot_vel: 0.1 default: 0.4  # this is the min angular velocity when there is negligible translational velocity

acc_lim_x: 0.1
acc_lim_y: 0.0      # diff drive robot
acc_lim_theta: 0.5 #1.5
decel_lim_x: -0.5 #1.5
decel_lim_y: -0.0
decel_lim_theta: -0.5 #1.5

